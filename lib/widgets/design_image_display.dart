import 'package:flutter/material.dart';

class DesignImageDisplay extends StatelessWidget {
  const DesignImageDisplay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
